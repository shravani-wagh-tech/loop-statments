#include <stdio.h>
int main()
{
    int num;
    int i;
    printf("enter a number:");
    scanf("%d",&num);
    for(i = 1; i <=1; i++)
    {
        if(num % 2 == 0)
        {
            printf("number is even");
        }
        else
        {
            printf("number is odd");
        }
        }
    return 0;
}